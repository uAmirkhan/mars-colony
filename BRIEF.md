# BRIEF — mars-colony

_Одно место правды о состоянии. Перезаписывается при каждом чекпоинте._

## Что это и зачем
Казуальный 3D сити-билдер на Марсе в визуальном языке Township. Домен и экономика — `mars-colony/src`, сцена и интерфейс — `../mars-unity` (Unity 6, URP, MCP-мост). Цикл работы — `/loop` по `loop/PROMPT-DODELKI.md`, ворота `../loop/tools/loop_gate.py`, команда из 11 ролей-агентов.

## Состояние сейчас (2026-09-06, утро, ночной цикл 2 идёт)
- **Интерфейс.** Счётчики, карточка цели, кнопка меню — холодное стекло; карточка цели и кнопка «ПОСЕЯТЬ» на 9-slice спрайтах кита Wenrexa (CC0, `mars-unity/Assets/UI/Kit/`) с тенью на мир и цоколем. В медальонах — иконки заказчика (монета, звезда). Числа на Nunito. **Заказчик забраковал цвета и вид** («не под игру»): следующий ход — генерация элементов по паспорту арт-директора, как иконки.
- **Маяк цели** над теплицей: пин с глифом `habitat-dome` (game-icons, CC BY), макушка по вершинам меша запечена в `QuestMarker`. Клик по зданию подтверждён настоящим кликом в Play.
- **Иконки ресурсов:** все 20 приняты (v2 промптов), вырезаны в `mars-unity/Assets/UI/Ikonki/resursy/`. Метан заменён на m5 (красный баллон с вентилем, S 67%, блик 1.35%): серая бочка FUEL стояла особняком.
- **Ночь 2 (06.09):** UI-20 склад на элементах заказчика по референсу амбара (без ячеек) — закрыт; UI-22 кликабельность — закрыт (найден и исправлен отсутствующий EventSystem: все кнопки были мертвы на клик); UI-23 шаттл — анимация летает (кадры с тенью), пыль в RT-кадрах не рендерится — отложено на утро; UI-24 (циан-контур с каймой, все 19 зданий, якорь плашки, карточка при складе) и UI-25 (нули склада, молнии под флагом `PokazyvatMolnii`, маяк цели при старте) — закрыты; судья: G 25/40, курс на продажу товара (UI-26, в работе). Заказчик ночью: контур выделения вернуть в яркий циан, проверить выделение всех зданий; панель склада отдельная, ячеек нет. Отчёт `loop/ui/OTCHET-nochi-2.md`, свод правил `loop/ui/SVOD-PRAVIL-UI.md`. MCP unity в сессии поднялся вместе с редактором; субагенты моста не имеют.
- **Интерфейс на генерированных элементах (пилот принят в кадр).** Заказчик сгенерировал в Gemini 12 картинок по промптам (`loop/promty/promty-elementov-pilot.md`, `promty-elementov-komplekt-gemini.md`); вырезка `tools/element_narezka.py` (маска по оттенку, `--dyrki` для полостей), спрайты в `mars-unity/Assets/UI/Elementy/`. Решение: тёплая палитра целиком (кнопка-пилюля a, капсула a, кремовая панель с лентой, синий сквиркл меню). Код: `InterfeysBuilder.ElementyVariant`, `KartochkaTseliGen`, `SchetchikGen`, `KnopkaMenyuGen`; batch-съёмка `PilotElementy` → `loop/ui/pilot-elementy-a.png`/`-b.png`. Бесплатный image-gen для UI закрыт (память). MCP unity в сессии 05.09 не поднялся — кадры через batchmode.
- **Вырезка нейросетью:** `tools/ikonka_rembg.py` (rembg, BiRefNet-lite) — все 20 иконок перевырезаны без подставок, в Unity. Цветовые эвристики закрыты. Заказчик забраковал синюю кнопку меню, глянцевый крест, тил-ячейку — перегенерирует; промпты отданы. Ночь 2: `loop/PROMPT-NOCH-2.md` (склад, замена элементов, иерархия, молнии, juice).
- **Медальоны счётчиков** (кадр `loop/ui/vitok-17-medalony.png`): монета и звезда заказчика подставлены через `SpriteResursa()` в `InterfeysBuilder.cs`, лог сборки подтверждает имена спрайтов. Заказчик спрашивал про инструменты (Figma MCP и т.п.) вместо рисования руками — ответ: конвейер генерации по паспорту, тот же, что дал иконки.
- **Инструменты:** `loop/tools/ui_zamer.py --fon` (маска интерфейса), `tools/ikonka_zamer.py`, `tools/ikonka_vyrezka.py`, `tools/kit_perekraska.py`.
- Ворота: закрыты UI-1..UI-15 кроме UI-13 (тень 11.1/12) и UI-16 (эскалация). Ledger — `../loop/ledger.json`.

## Следующие шаги (сверяй с bd ready)
1. Паспорт элементов интерфейса от `mars-art-director` (в работе) → промпты → генерация заказчиком → вырезка/9-slice → пилот на карточке цели, парный кадр.
2. Меню склада по референсу `../raw/Playrix/referens-sklad.png` — спека `mars-ux` (в работе) → реализация `mars-unity-coder`.
3. Решения заказчика: язык элементов (тёплый/холодный), бейджи-молнии.

## Блокеры
- Слово заказчика по палитре элементов (две ветки паспорта) и по молниям.
- Спека склада (`mars-ux` → `loop/ui/spec-sklad.md`) и паспорт элементов (`mars-art-director` → `loop/promty/pasport-elementov-interfeysa.md`) — первый запуск 04.09 упал по лимиту API без результата; перезапущены 05.09 утром на sonnet, ещё не вернулись.

## Решения и грабли (append-only)
- 2026-09-04 — весь HUD на непрозрачное стекло — полупрозрачный визор над оранжевым грунтом не даёт холодного тона (арифметика в Linear), только над льдом.
- 2026-09-04 — иконки: «matte dusty» и слова оборудования в промпте дали тусклые предметы с зажимами; палитра мира не палитра товара; реестр слов — `loop/promty/REESTR.md`.
- 2026-09-04 — не рисовать элементы из примитивов: заказчик забраковал дважды; искать готовое (game-icons, киты CC0) или генерировать по паспорту.
- 2026-09-04 — макушка объекта: bounds занижают на 37 px, bbox углов завышает на 51; верно только по вершинам меша; меш без Read/Write читается в редакторе, не в Play — запекать.
- 2026-09-04 — `TextureImporter.spriteMeshType` в Unity 6 нет (через `TextureImporterSettings`); сборка молча падала, редактор жил на старой сборке — печатать в лог то, что могло измениться только новой сборкой.
- 2026-09-04 — клик в Play: активировать окно Unity (иначе клик уходит в VS Code), `DisableDomainReload` чтобы мост MCP выжил.
- 2026-09-06 — объект, созданный старым кодом, живёт в сцене: правка параметров ParticleSystem не действует, пока объект не пересоздан; Unity держит ≤8 бёрстов, оси velocityOverLifetime в одном режиме.
- 2026-09-06 — субагенты не видят MCP-мост Unity: при открытом редакторе они правят код, кадры снимает оркестратор через RunCommand; batch при открытом редакторе нельзя.
- 2026-09-05 — вырезка предмета из генерации — только нейросетью (rembg): три цветовые эвристики брали тень-подставку и резали серые детали; готовый инструмент раньше своего.
- 2026-09-05 — Bash-инструмент разворачивает `\n` в heredoc даже в кавычках: литерал слэша собирать `bytes([92])`; проверять байтами.
- 2026-09-05 — фоновые агенты падают по лимиту сессии молча (пустой output): после лимита проверять файлы результата, не верить, что «в работе».
- 2026-09-04 — замер по прямоугольнику меряет мир между элементами; `ui_zamer --fon` считает по маске; рамка пишется рядом с числом.
