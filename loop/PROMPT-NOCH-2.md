# Ночной цикл 2: интерфейс на элементах заказчика, экран склада

Продолжение цикла 2026-09-04/05. Что сделано днём 05.09 — `mars-colony/BRIEF.md`,
кадр `loop/ui/pilot-elementy-a.png`. Здесь — что делать ночью без заказчика.

Границы со слов заказчика: `loop/ui/INTERVYU.md` плюс решения 05.09:
- **Язык интерфейса — тёплый Township.** Кремовые панели с лентой, зелёная
  пилюля действия, капсулы тёмного стекла под счётчики, цвет только у предметов
  и кнопок действия. Референс — `raw/Playrix/tawnship_train/photo_2026-07-31_00-18-10.jpg`.
  Синий сквиркл меню, глянцевый красный крест и тил-серая ячейка заказчик
  забраковал как «слишком яркие, забирают внимание»: их он перегенерирует.
- **Элементы не рисуются кодом.** Только картинки из `mars-unity/Assets/UI/Elementy/`
  (панель, кнопки a/b, капсулы a/b, бар трек и заливка, ячейка, крест, замок,
  меню) и иконки `Assets/UI/Ikonki/resursy/`. Нужен новый элемент — `escalate`
  с готовым промптом в `loop/promty/promty-elementov-komplekt-gemini.md`,
  строку не закрывать.
- **Вырезка — только нейросетью:** `python mars-colony/tools/ikonka_rembg.py <файл> --out <папка>`
  (иконки) или с `--bez-obvodki --ne-kvadrat` (элементы). Цветовые эвристики
  (`ikonka_vyrezka.py`, `element_narezka.py`) не чинить и не применять к новым
  картинкам: они брали тень-подставку и режут серые детали.

## Команда

Роли и правило «правит один, принимает другой» — как в `PROMPT-DODELKI.md`
(таблицы там актуальны). Добавка: `mars-ux` отвечает на расхождения спеки склада
с реальностью, `mars-unity-coder` собирает экран, `mars-ui-artist` кладёт
элементы, `mars-art-inspector` принимает кадр, `mars-tester` ломает склад.

## Строки работы, по убыванию важности

1. **Экран склада** по `loop/ui/spec-sklad.md`, ворота UI-20. Спека писалась под
   тёмное стекло (её раздел 0) — палитру брать из пилота: кремовая панель с
   лентой (`panel.png`, заголовок «СКЛАД» на ленте коричневым), ячейки
   `yacheyka.png` (или `yacheyka-2.png`, если заказчик прислал), крест
   `knopka-krest.png` (или `-2`), замок `zamok.png`, иконки товаров без
   подставок. Композиция, сетка, состояния, тексты, тап-таргеты — из спеки как
   есть. Открывается кнопкой меню и тапом по зданию склада, закрывается крестом и
   тапом по затемнению. Порядок: каркас и сетка с реальными запасами из
   `mars-colony/src` → состояния (пусто, полно) → продажа степпером → juice
   открытия. Каждый шаг — свой виток и свой кадр с открытым складом.
2. **Всё кликабельно** (`bd` mars-colony, P0, заказчик 05.09 ночью): тап по любому
   зданию — выделение объекта (подсветка/контур, не перекраска) и плашка с
   названием над макушкой (макушка по вершинам меша, как у маяка цели). Плашка —
   на элементах заказчика (капсула или крем), название из реестра зданий.
   Порядок: `mars-unity-coder` raycast и состояние «выбрано/снято», затем
   `mars-ui-artist` вид, `mars-juice` появление, `mars-tester` (два тапа подряд,
   тап в пустоту, тап во время открытого склада), кадр с выделенной теплицей и
   настоящий клик в Play как в UI-14. Ворота UI-22.
3. **Анимация шаттла** (`bd`, P1): прилёт по дуге из-за верхнего края, посадка на
   площадку с пылевым облаком, пауза, взлёт и уход за кадр. Без внешних пакетов:
   корутина и `AnimationCurve`, `ParticleSystem` для пыли. Исходную позу модели
   снять и восстанавливать. Кадры трёх фаз (в воздухе, касание, взлёт) — снимать
   с фиксированным временем анимации через batch-метод. Ворота UI-23.
4. **Новые картинки заказчика.** Перед каждым витком смотреть
   `C:/Users/amirkhan/Downloads/` на файлы новее `loop/ui/pilot-elementy-a.png`
   с именами `Gemini_Generated_Image_*.jpg` или `*.jpg` от Gemini. Появились —
   вырезать `ikonka_rembg.py --bez-obvodki --ne-kvadrat`, посмотреть глазами,
   опознать по форме (сквиркл с полосками → `knopka-menyu`, круг с крестом →
   `knopka-krest`, квадратное углубление → `yacheyka`), положить в
   `Assets/UI/Elementy/` под именем с суффиксом `-2`, переключить в
   `InterfeysBuilder` и снять кадр. Ворота UI-21.
5. **Иерархия:** карточка цели весит больше строки счётчиков (`bd` mars-colony-njy).
   Инструмент: размер шрифта/тень/цоколь, не цвет. Ворота UI-17 (пересоздать).
6. **Бейджи-молнии** (UI-16 escalated, `bd` mars-colony-mos): убрать до появления
   механики энергии — обратимо, в коде оставить флаг. Отметить в отчёте как
   решение цикла, заказчик утром подтвердит или вернёт.
7. **Juice маяка цели** (`bd` mars-colony-7h0): вертикальный бег ±8 px за 1.6 с,
   гашение при смене состояния. `mars-juice`.
8. **Тень карточки** UI-13 (open, 11.1/12): дотянуть на новой панели или закрыть
   как неактуальную, если тень новой панели читается.

## Виток

1. **Сборка и кадр без MCP:** редактор Unity не запущен, мост в этой сессии не
   поднят. Кадр снимать batch-процессом, один за раз:
   `"C:/Program Files/Unity/Hub/Editor/6000.0.64f1/Editor/Unity.exe" -batchmode -quit -projectPath "C:\Ai\Jarvis\mars-unity" -executeMethod InterfeysBuilder.PilotElementy -logFile "C:\Ai\Jarvis\mars-colony\loop\ui\noch2-batch.log"`
   Кадры — `loop/ui/pilot-elementy-a.png` (вариант a — рабочий). Для склада
   добавить метод `InterfeysBuilder.KadrSklad()`, который открывает экран и
   снимает `loop/ui/noch2-sklad-<n>.png`. Перед batch — `grep "error CS"` в логе
   прошлого запуска; компиляция падает молча, редактор живёт на старой сборке.
   Второй инстанс Unity не поднимать: `Temp/UnityLockfile` убивает оба.
2. Одна ПРИЧИНА на виток. Критерий ДО правки:
   `python loop/tools/loop_gate.py attempt UI-<n> --criterion "..."`, рамка
   замера рядом с числом. Замер: `loop/tools/ui_zamer.py --fon`.
3. Двойная приёмка: метрика дошла И `mars-art-inspector` не нашёл сломанного рядом.
   `loop_gate.py done UI-<n> --verified-by "..."`.
4. Каждый третий виток — критик (`mars-critic-kadr` / `mars-critic-igrok`),
   каждый пятый — `mars-judge`. После критики `mars-uchenik` пишет правило.
5. Правки в `.cs` — питоном с байтовой проверкой; обратный слэш собирать через
   `chr(92)`: Bash-инструмент разворачивает `\` даже в кавычечном heredoc.
6. Каждые три витка — чекпоинт dev-memory: `BRIEF.md`, `bd`, `bd export`,
   `memory/log.md`, коммиты в `mars-colony` и `mars-unity`.

## Запреты

- Не рисовать элементы кодом и примитивами. Нет картинки — `escalate` с промптом.
- Не подгонять кадр под метрику; не переписывать критерий под результат.
- Три попытки на строку. Упёрлось в решение заказчика — `escalate`, следующая строка.
- Экономику, позиции зданий, палитру не трогать: это константы заказчика.
- Не останавливаться. Закончились строки — `bd ready` в `mars-colony`, берётся
  верхняя по приоритету, не связанная с 3D-сценой.

## Утро

Отчёт `loop/ui/OTCHET-nochi-2.md`: что закрыто (ворота, кадры), что escalated и
с каким промптом, решения цикла на подтверждение (молнии), три главных кадра.
