# Арт-инспектор — промежуточное состояние (узел 0)

## Сделано

- Прочитан `run-7/plan.md`, раздел про шкалу V в `run-6/judge.md`, свой отчет `run-6/art-inspector.md`.
- Найден текущий кадр витрины: `mars-unity/colony3d-web.png` (432735 байт, дата
  06.08 16:46) — это тот же файл, что использовался в прогоне 6, байт-в-байт
  (размер и mtime совпадают с архивом `metrics-archive/metrics-scene3d-2026-08-06T13-46-52.json`).
  Свежего кадра для прогона 7 НЕТ. `mars-unity/colony3d-scene.json` тоже не
  менялся (та же дата 06.08 16:46) — сцена не редактировалась с прогона 6,
  поэтому старый кадр валиден как замер "до правок", но это не новый рендер.
- `Assets/Editor/SceneBuilder3D.cs` имеет mtime 09.08 00:34 (сегодня) —
  проверены байтом все четыре места, относящиеся к пяти вычетам: код
  идентичен тому, что описан в `run-6/art-inspector.md`. Не проверял файл
  целиком построчно — вне зоны узла 0, но зона сцены (`colony3d-scene.json`)
  физически не тронута, так что рендер, который я оцениваю, соответствует
  текущему коду.
- Перемерены все пять вычетов независимо (числа и координаты см. в
  `art-inspector-baseline.md`), все пять живы, суммарно -44, как и в
  `run-6/judge.md`.
- Найден готовый тестовый прогон конвейера GLB->FBX от программиста:
  `mars-unity/model-intake.json` и `mars-unity/incoming-fit.png` (оба датированы
  09.08 сегодня). `model-intake.json` -> `textures: []` — текстуры на этом
  тесте НЕ пережили конвертацию, `incoming-fit.png` показывает белую
  нетекстурированную заготовку шаттла. Зафиксировано как контекст для узла 1,
  не как моя находка (зона программиста).
- Просмотрены `design/models/osmotr/*.png` (каталог партии, шаттл в двух
  разрешениях, дрон-курьер) и `design/models/shuttle-v2-1536.glb` через рендеры
  каталога. Численно измерена разница стиля покраски между китом сцены и
  партией (метод — см. baseline-отчет, раздел "Критерий склейки").
- Отчет `art-inspector-baseline.md` записан.

## Следующий шаг

Узел 1: гейт склейки на одной модели грунта в клетке (6,6). Применить
зарегистрированный здесь критерий к СВЕЖЕМУ кадру (не к osmotr-рендерам).

## Открытые вопросы / блокеры для меня

Нет. Все данные для узла 0 получены.
