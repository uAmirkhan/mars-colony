# Спецификация vs код — сверка, прогон 7 (ночной узел)

Дата: 2026-08-10
Роль: рецензент соответствия (только чтение, ничего не правит)

## 1. Неизменность домена (src/domain, src/state, src/sim, src/ui) с f829a1d

Проверка: `git diff --stat f829a1d..HEAD -- src/domain src/state src/sim src/ui` — пусто (ноль строк) по всем четырем каталогам отдельно.

`git log --oneline f829a1d..HEAD -- src/` — единственный коммит, задевающий `src/`: `bc6bb9b` («Прогон 7, день 1: приемка партии числами, конвейер GLB-FBX, разведка ландшафта и инструментов, фундамент-сцена»).

Полный список файлов `src/`, измененных в bc6bb9b (`git diff --name-status f829a1d..HEAD -- src/`), — три файла, все добавлены (A), ни одной модификации существующего файла:

```
A  src/design-tools/__tests__/defects-run7-plita.test.ts        (80 строк)
A  src/design-tools/__tests__/defects-run7-polybudget.test.ts   (68 строк)
A  src/design-tools/__tests__/glb-footprint.ts                  (201 строка)
```

Вывод: домен (`src/domain`, `src/state`, `src/sim`, `src/ui`) не менялся ни одной строкой с вердикта судьи прогона 6. Единственные затронутые файлы — новые тесты ломателя `defects-run7-*.test.ts` плюс вспомогательный модуль `glb-footprint.ts` (не сам тест, а копия матричной математики `design/tools/check-model.mjs`, используемая только из `defects-run7-plita.test.ts` — `import { footprintOccupancy } from './glb-footprint'`). Формально имя `glb-footprint.ts` не начинается с `defects-`, но по факту это неотделимая часть того же теста-ломателя (используется только им, живет в том же `__tests__`), а не отдельная фича. Отмечаю как пограничное наблюдение, не как находку: код не выходит за пределы каталога `__tests__`, который исключен соглашением.

Расхождений сверх заявленного не найдено.

## 2. spec-drift.test.ts — точечный прогон

Команда: `npx vitest run src/domain/__tests__/spec-drift.test.ts`, HEAD = `bc6bb9b160a0a72149fece5a8d6917f0c39976d1`.

Результат:
```
RUN  v4.1.10 C:/Ai/Jarvis/mars-colony
 Test Files  1 passed (1)
      Tests  146 passed (146)
```

Зеленый, 146/146. Контракт «имя параметра спеки существует в коде» подтвержден на текущем HEAD. Поскольку домен не менялся с f829a1d (см. п.1), этот прогон эквивалентен прогону на состоянии, зафиксированном вердиктом судьи прогона 6, — новых расхождений внести было неоткуда.

## 3. Витрина mars-unity (658cb94, 65735c6) vs заявленное в сообщениях коммитов и в scene-artist-2.md

Репозиторий `mars-unity` — отдельный git-репозиторий (`c:\Ai\Jarvis\mars-unity`), не подмодуль `mars-colony`. Коммиты:

- `658cb94` "Третий ярус DeepTop=-1: карьер ниже уровня земли"
- `65735c6` "Карьер на (3,1) по расчету видимости, полная глубина -2.0"

### 3.1 Числа уровней (LowTop/HighTop/DeepTop)

Спецификация задания: третий ярус ниже нуля, "лестница" уровней. Код (`SceneBuilder3D.cs:910-912`):
```
private const float LowTop = 0f;
private const float HighTop = 1f;
private const float DeepTop = -1f;
```
`git diff 658cb94..65735c6 -- Assets/Editor/SceneBuilder3D.cs` не задевает эти три строки — константы введены в 658cb94 и не менялись во второй правке. Совпадает с заявлением scene-artist-2.md («Не менял константы LowTop/HighTop/DeepTop»). Расхождений нет.

### 3.2 Пивот 'M' => -2.0

Отчет `scene-artist-2.md` (шаг 8): «Пивот `M` вернул на −2.0 (`DeepTop − 1`, полная глубина по замыслу)». Код (`SceneBuilder3D.cs:1210-1216`, финальный свич пивота):
```
float pivot_y = c switch
{
    'M' => -2.0f,
    'Q' => DeepTop,
    'R' => DeepTop,
    _ => 0f,
};
```
Совпадает дословно. Комментарий тут же (строка 1171) подтверждает формулу: «пивот = DeepTop − 1 = −1 − 1 = −2.0».

### 3.3 Клетка карьера в TerrainMap — переезд на (3,1), пандус R

Отчет (шаг 8): «(3,1): было `c`, стало `M`»; «(3,2): было `L`, стало новым символом `R`»; «(4,7): было `M`, вернул к исходному `h`»; «(3,7): было `Q`, вернул к `L`».

Код (`SceneBuilder3D.cs:1081-1091`, `TerrainMap`, индексация `[j][i]`):
```
"....cb..",   j=0
"...MLLLb",   j=1  -> i=3 = 'M'   => (3,1) = M
"..cRLLLL",   j=2  -> i=2='c', i=3='R'  => (3,2) = R, (2,2) не тронут
".cLLLLLL",   j=3
".LLLLLCS",   j=4
".LLLSTTT",   j=5
".LLLTTTT",   j=6
"..dLhTTe",   j=7  -> i=3='L', i=4='h' => (3,7) = L, (4,7) = h
```
Все совпадает с отчетом дословно: `(3,1)=M`, `(3,2)=R`, `(4,7)=h`, `(3,7)=L`.

Символ `R` также добавлен в `Tile()` (`'R' => ("terrain_slope", 90f)`, строка 1137) и участвует в свиче пивота (`'R' => DeepTop`, строка 1214) — оба места, о которых заявлено в отчете («добавлен новый символ R в Tile() и в свич пивота»). Расхождений нет.

### 3.4 world_max.y = -0.927 по финальному colony3d-scene.json

Сообщение коммита `65735c6`: «world_max.y = -0.927 (было -0.077 на 4,7)». `colony3d-scene.json` (мтайм 2026-08-10 02:01:06, синхронно с остальными файлами финальной пересборки):
```
{ "id": 4, "name": "terrain_mining_3_1", ..., "world_max": [0.000, -0.927, -4.000], ... }
```
Число совпадает дословно с текстом коммита и с отчетом (таблица шага 11: «world_max.y: −0.927»). Расхождений нет.

### 3.5 Обновленные комментарии

Проверены комментарии у `TerrainMap` (строки 986-1080) и у свича пивота в `BuildTerrain` (строки 1159-1209) — оба содержат актуальную историю (переезд (4,7)→(3,1), три замера 8.7%/3.9%/0.1%, формулу пивота, ссылку на `scene-artist-2.md`) и текущие числа (`world_max.y = -0.927`, «дно на DeepTop=−1, видимый перепад к низкой земле — полный метр»). Согласуется с заявлением отчета «обновлены все затронутые комментарии свежими числами». Расхождений нет.

### 3.6 Найденные расхождения (мелкие)

**Находка А (не в домене веб-игры, не гейтится spec-drift — 3D-витрина Unity).** `scene-artist-2.md`, шаг 8 (строки 421-424 отчета) утверждает про размещение `rock_crystalsLargeB` у новой ямы:

> «намеренно ВНЕ диапазона монорельса по `i` (монорельс начинается с `i=4.0`, этот камень на `i=3.7`, **зазор целая клетка**)»

Код (`SceneBuilder3D.cs:1747-1751`, тот же коммит) сам же считает иначе:

> «путь начинается с `i=4.0` (здесь `i=3.7`, **зазор 0.3 = 0.6 мировых**)»

`4.0 − 3.7 = 0.3` единицы `i`, и при `Grid = 2.0f` (`SceneBuilder3D.cs:909`) это 0.6 мировых единиц — меньше ОДНОЙ клетки сетки (клетка = 1.0 по `i` = 2.0 мировых), а не «целая клетка», как написано в тексте отчета. Расхождение текста отчета с числом, которое дает тот же автор в комментарии кода того же захода. Серьезность низкая: сам вывод («коллизии нет») не оспаривается — 0.3 клетки все еще вне диапазона трассы (i от 4.0 до 6.51) — но формулировка «зазор целая клетка» количественно неточна против собственного числа кода.

Других расхождений между текстом отчета `scene-artist-2.md` и сообщений коммитов `658cb94`/`65735c6`, с одной стороны, и фактическим состоянием `SceneBuilder3D.cs`/`colony3d-scene.json`, с другой, не найдено.

### 3.7 Известное (не новое) расхождение commit-текста с воспроизводимым числом — для полноты

`tester-final.md` (написан раньше, независимо) уже зафиксировал: сообщение коммита `658cb94` заявляет «`world_max.y`: было +1.073, стало **-0.027**», но пересборка того же коммита без единой правки стабильно дает **-0.077** (дважды подряд), расхождение 0.05. `scene-artist-2.md` (строка 270 таблицы) независимо подтверждает то же самое: «−0.077 (воспроизведено; коммит заявлял −0.027, не воспроизводится, находка 2 ломателя)». Это уже задокументированная находка тестера, не новая с моей стороны, — привожу для полноты сверки текста коммита с фактическим числом, как просил пункт 3 задания. Направление фикса (карьер ниже нуля) не меняется в обоих числах, дрейф изолирован на меше карьера, причина не установлена ни тестером, ни художником (оба явно пишут «не моя роль чинить»).

## 4. Инвентарь входов судьи — mtime и согласованность по времени

Вердикт судьи прогона 6 (`f829a1d`) закоммичен `2026-08-06 20:08:50 +0300`. Все перечисленные файлы датированы `2026-08-09`/`2026-08-10` — свежее вердикта. Порядок по времени (по возрастанию mtime):

| файл | путь | mtime (local, +03:00) |
|---|---|---|
| tester-final.md | mars-colony/loop/run-7/ | 2026-08-10 01:11:54 |
| art-inspector-final.md | mars-colony/loop/run-7/ | 2026-08-10 01:43:32 |
| colony3d-scene.json | mars-unity/ | 2026-08-10 02:01:06 |
| colony3d-web.png | mars-unity/ | 2026-08-10 02:01:35 |
| metrics-3d.json | mars-unity/ | 2026-08-10 02:01:35 |
| scene-artist-2.md | mars-colony/loop/run-7/ | 2026-08-10 02:03:59 |
| art-inspector-verdict2.md | mars-colony/loop/run-7/ | 2026-08-10 02:15:03 |

Согласованность: `colony3d-scene.json`, `colony3d-web.png`, `metrics-3d.json` (плюс `colony3d-editor.png`, `colony3d-ids.png`, `colony3d-objects.txt`, `metrics-scene3d.json` — не запрошены явно, но лежат в том же кластере 02:01:06-02:01:35) записаны одной и той же финальной пересборкой (`measure3d.mjs`, `at: "2026-08-09T23:01:27.852Z"` UTC = `2026-08-10 02:01:27` local — внутри 30-секундного окна mtime-кластера). `scene-artist-2.md` дописан ПОСЛЕ этой пересборки (02:03:59) и цитирует именно ее числа (`world_max.y: -0.927`, `stack_suspects: 25`, `red_total: 0` — совпадает с содержимым `metrics-3d.json`, см. п.3.4 и текст выше). `art-inspector-verdict2.md` дописан последним (02:15:03) и явно ссылается на тот же снимок («по кропу... на свежем colony3d-web.png»), файл `colony3d-web.png` на диске не перезаписывался между 02:01:35 и 02:15:03 (единственный файл с этим именем в дереве — см. поиск `find` по `colony3d-web.png`, ровно один результат).

`tester-final.md` и `art-inspector-final.md` датированы РАНЬШЕ финальной пересборки — оба описывают состояние ДО правки (4,7)→(3,1) и корректно называют себя отчетами по промежуточному/багованному состоянию (первый — по коммиту `658cb94`, второй — по снимку 01:13:53, тоже до `65735c6`). Это ожидаемо: они входы для художника, а не для финального вердикта, и логически предшествуют финальной пересборке.

Вывод: набор входов свежий (все позже `f829a1d`) и внутренне согласован по времени — файлы, описывающие финальное состояние (`scene-artist-2.md`, `art-inspector-verdict2.md`), написаны строго после файлов финальной пересборки, которые они цитируют, а не до них. Расхождений не найдено.

## Дополнительная проверка: гейт и статус тестов ломателя

`npm run test:gate` (`vitest run --exclude "**/*defects-*.test.ts"`) — **609/609 зеленых**, 24 файла. Подтверждает, что исключение `defects-*` из гейта реально работает (не просто заявлено соглашением), и что домен в целом (все файлы, кроме двух `defects-run7-*.test.ts`) проходит полный набор тестов.

Отдельно `npx vitest run defects-` — оба новых теста ломателя КРАСНЫЕ (`defects-run7-plita.test.ts`, `defects-run7-polybudget.test.ts`), 2 failed из 57 в этой группе. Это ожидаемо и согласуется с сообщением коммита `bc6bb9b` («Тесты ломателя: дефект плиты и нестыковка бюджета треугольников доказаны красным») — тесты документируют реальные дефекты инструментов приемки 3D-моделей (`design/tools/`), не регресс домена игры, и намеренно остаются красными вне гейта.

## Итог

1. **Неизменность домена доказана.** `src/domain`, `src/state`, `src/sim`, `src/ui` — ноль измененных строк с `f829a1d`. Единственные тронутые файлы в `src/` — три новых файла в `src/design-tools/__tests__` (два теста-ломателя `defects-run7-*` и вспомогательный модуль `glb-footprint.ts`, используемый только одним из них). Расхождений сверх заявленного нет.
2. **spec-drift.test.ts зеленый**, 146/146, на HEAD `bc6bb9b`.
3. **Витрина mars-unity сверена и совпадает** с сообщениями коммитов `658cb94`/`65735c6` и с `scene-artist-2.md` по всем проверенным числам (константы уровней, пивот `M`=-2.0, клетка карьера в `TerrainMap`, `world_max.y`=-0.927, обновленные комментарии) — с одним мелким расхождением текста отчета с числом в коде того же автора (п.3.6, «зазор целая клетка» против фактических 0.3 клетки/0.6 мировых) и одним уже известным (задокументированным тестером, не новым) расхождением текста коммита `658cb94` с воспроизводимым числом (`-0.027` в тексте против стабильно воспроизводимых `-0.077`, п.3.7).
4. **Входы судья-комплекта свежие и согласованные по времени** — все позже `f829a1d`, файлы финальных выводов (`scene-artist-2.md`, `art-inspector-verdict2.md`) написаны строго после файлов финальной пересборки (`colony3d-scene.json`/`colony3d-web.png`/`metrics-3d.json`), которые они цитируют.

Домен игры (числа, имена параметров, состояния/переходы, критерии приемки ТЗ, инварианты И-1..И-15) не проверялся заново по существу в этом прогоне — в этом нет необходимости, поскольку код домена не менялся ни на одну строку с вердикта прогона 6 (п.1), а тот вердикт уже дал заключение по существу. Пересверка содержательных пунктов ТЗ против домена, не изменившегося с прошлого раза, добавила бы найденное с прошлого раза, а не новое, и это явно не то, что просил заказ этого прогона (узкий скоуп: доказать неизменность и сверить только тронутое).

