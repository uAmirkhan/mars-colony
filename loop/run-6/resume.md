# Точка возврата — прогон 6, остановлен владельцем 2026-08-06

Работа остановлена по команде для перезапуска Claude Code. Читать этот файл
первым.

## Состояние: чистое, все закоммичено

Последний коммит — `47ce9e5`. Рабочее дерево пустое, ничего не потеряно.

Проверки на момент остановки: **гейт 583 зеленых, доказательства дефектов 50
зеленых**. Браузерная проверка (`npm run e2e`) не прогонялась после последних
правок — прогнать первым делом, до любой новой работы. Прошлый полный прогон
давал 100 из 100.

## Где цикл находится

Судья выставил прогон 5 (`loop/run-5/judge.md`), впервые за все время с полным
комплектом входов:

| Шкала | Прогон 4 | Прогон 5 |
|---|---|---|
| S Сборка | 100 | 100 |
| V Сцена | нет данных | 49 |
| G Играбельность | 100 | 100 |
| C Код | 0 | 30 |
| T Спека | 0 | 100 |

Порог 80 взят по S, G и T. **Минимальная шкала — код (30), туда и идет прогон
6.** Не тупик: C выросло на 30 за переход.

## Что доделать в первую очередь

Программист остановлен на середине задания, его план по пунктам с отметками —
`loop/run-6/coder-work.md`. Пункт 1 сделан и закоммичен, пункты 2 и 3 нет:

1. **Сделано.** Property-тест докупки строй-модуля за изотопы: действие
   `buy_module` в общей последовательности плюс инвариант «докупленное не
   уходит в минус и вместе со складом не превышает рецепт». Снимает вычет 10.
2. **Не начато. И-10, реализуемость заказа (вычет 20).** Константа
   `ORDER_FEASIBILITY_DEADLINE_SHARE` объявлена в `domain/config/economy.ts` и
   не читается нигде. В журнале программиста лежит проработанный план: новая
   функция `totalProductionMinutes` в `rushcost.ts`, флаг `ACHIEVABILITY_CHECK`
   по механикам, вызов ребаланса в генераторе шаттла. Там же его решение по
   FTUE (первый рейс структурно не может уложиться в бюджет) — **это решение
   обязано попасть в реестр пунктом 24, иначе вычет не снимется.**
3. **Не начато. И-13, изоляция дефицита между механиками (вычет 30).** Поиск
   `DeficitLock` по `src/` дает ноль вхождений. Инвариант откладывали на
   прогоне 2 с формулировкой «при одной механике доставки он пустой» — механик
   теперь две, и он снова считается. Самый большой кусок, возможно поднятие
   `SAVE_VERSION` (сейчас 4).

Плюс четвертый вычет по коду, который судья взял решением, а не строкой
рубрики: интермиттентный срыв сборки состояния показа под шестью параллельными
браузерами, примерно дважды из семидесяти двух. Причина НЕ найдена. Сделано
все, чтобы следующий случай объяснил себя сам: сборка называет причину срыва в
консоль, а браузерные проверки сохранения ее слушают и подставляют в текст
падения. Ловить — на следующем красном прогоне.

## Что ждет владельца

- **Реестр открытых вопросов вырос до 23 пунктов**
  (`../wiki/saas/projects/mars-colony/spec-prototype-build.md`, раздел 8).
  Ждут решения: 7, 10, 11, 14, 17, 18, 19, 20, 21, 22, 23.
- Свежий и важный — пункт 23: изотопы обозначены на одном экране двумя знаками
  сразу, шестигранником в шапке и символом атома на кнопках, документы
  противоречат друг другу, а сам символ атома на Windows рисуется цветком.
- **Очередь моделей:** шаттл первым, затем купол-оранжерея. Приемка готова:
  из папки `mars-unity` — `node fit-model.mjs путь\к\модели.fbx --shoot`.
  По оценке инспектора купол закрывает самую дорогую находку сцены: холодных
  пикселей в кадре 0.4 процента после перекраски солнечных панелей.

## Ловушки

- `SAVE_VERSION` = 4. Любая правка формы состояния обязана его поднимать.
- `missingFor` принимает `BuildSlot`, а не `BuildKind`: иначе не видит
  докупленное и рисует дефицит на оплаченном модуле.
- Диалог подтверждения траты живет НА ПАНЕЛИ, не в карточке. В карточке он
  отрывается от документа посреди нажатия, когда карточка перестраивается по
  таймеру.
- Модалка — верхний слой (60), выше шапки (40) и выключателя звука (50).
  Опустишь обратно — крестик закрытия уедет под шапку.
- Метрики сборки архивируются с отметкой времени в `mars-unity/metrics-archive/`.
