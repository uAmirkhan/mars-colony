# Отчёт ночи 3 — играбельная колония (06.09.2026)

Спека: `../SPEC-NOCH-3-igra.md`. Записи по спринтам, свежие снизу.

## 02:40 — старт
- Спека утверждена кликом. Порядок: хвосты вечера (огонь шаттла, шнек, хореография дронов после починки паузы) → спринт 1 грядки → 4 склад → 9 HUD/цель → 2 фабрики → 7 уровни → 5 шаттл → 6 дроны → 3 добыча → 8 ускорения → 10 сохранение.
- Уже в Unity: `Scripts/Domain` (Types, Production: грядки, Warehouse, Config Goods/Economy/Power/Progress), `ColonyGame`/`ColonyState`, клики `BuildingClickTarget`, экран склада, HUD, маяк цели.

## 06:05 — спринт 1 «Грядки», первый проход
- Новое: `IgraKolonii` (состояние домена, часы по реальному времени × ускорение, сохранение в PlayerPrefs, панель по выбору здания), `PanelZdaniya` (панель действий из элементов экрана склада), маркер над теплицей. `VyborZdaniy` даёт события Vybrano/Snyato и не трогает мир при клике по UI; экран склада читает склад из домена и продаёт через домен.
- Проверка в Play (выбор теплицы через SendMessage, кнопка через onClick): панель открылась со списком 6 культур (время, цена, урожай, XP), «ПОСЕЯТЬ» → водоросли растут 2:00, кредиты 50→49. Кадры `loop/ui/noch3/s1-*.png`, монтаж `noch3-s1.png`.
- Грабля: перекомпиляция в Play перезагружает домен, Awake не повторяется — инициализация вынесена в OnEnable/Update.
- Открыто: маркер над теплицей на кадре не виден — проверяю; настоящий клик мышью для приёмки — позже пакетом.

## 06:10 — спринты 1 и 4 проходят контур
- После перекомпиляции в Play состояние восстановилось из сохранения (грядка READY, кредиты 49): критерий 4 в части таймеров работает.
- Маркер над теплицей: «ГОТОВО» с иконкой урожая (после починки перевода экранных координат в холст Screen Space Camera — без камеры холста точка уезжала на миллионы единиц).
- Собрать → +4 водоросли, +4 XP; склад показывает 4/50, попап «Продать за 8 кр.» → кредиты 49→57, склад 0. Кадры `noch3/s1-gotovo.png`, `s4-sklad-popup.png`, монтаж `noch3-s1b.png`.
- Починено: строки панели копились после перезагрузки домена (сироты без списка) — панель чистит детей по имени; маркер без спрайта показывал белый квадрат — Image гасится.

## 06:25 — домен перенесён, рантайм расширен на все механики
- Агент-программист вернул порт TS-домена: Drone, Shuttle, DropRoller, RushCost, DeficitLock, Tuning (time_scale), Mining (новое), Economy/Goods/Modules/Levels; фабричные слоты и Sell в Production. Тесты NUnit в `Assets/Editor/Tests/Domain/` (46 тестов) — прогон через Test Runner ещё впереди.
- `IgraKolonii` v2: состояние игры `SostoyanieIgry` (колония + фабрики по зданиям + площадки добычи + рейс шаттла + доска дронов + модули), сохранение Newtonsoft, панели для теплиц, фабрик (пищевой, плавильня льда, кислородная скважина), добычи (лёд тремя машинами, реголит), шаттла (отсеки, «ЗАГРУЗИТЬ», отправление по последнему отсеку, возврат по таймеру с новым заказом), дронов (доска заказов, «ОТПРАВИТЬ»/«ВЫБРОСИТЬ», полёт курьера к жилому куполу и награда по возврату), склад открывается по тапу.
- Хуки в сцене: PolyotShattla ждёт `ZagruzkaZavershena` и умеет `PriletetSnova`; MashinyDobychi бурит только при `DobychaLdaIdet/DobychaRegolitaIdet`; MarshrutyDronov в игровом режиме — курьер по заказу (`Poletet`).
- Нет текстильного модуля в сцене → ткань и комбинезон недоступны (отмечено).

## 06:27 — сквозной сценарий (критерий 1 при ×10)
- Панели всех зданий открываются и отвечают: шаттл (отсеки), пищевой завод (рецепты с входами), ледосборы (запуск партии, 20 с при ×10), дроны (доска из 4 заказов). Кадры `noch3/s5-shattl.png`, `s2-fabrika.png`, `s3-led.png`, `s6-dron.png`.
- Починено до сценария: заказы просили метан/руду/хлопок, которых негде добыть — список доступных товаров теперь строится из фабрик сцены и площадок добычи.
- Запущен скриптовый сценарий `noch3/e2e/`: посев трёх теплиц → сбор → батончики → лёд → вода → дрон → шаттл; итог ниже.

## 06:35 — сквозной сценарий пройден (×10)
- `e2e`: посев трёх теплиц (кредиты 50→47) → водоросли и соя собраны (+XP, уровень 1→2) → пищевой завод: два батончика (30 с) → лёд тремя машинами (20 с) → вода на плавильне (60 с) → уровень 3→4. Итог: кредиты 47, XP 58, склад 14/50.
- `e2e2`: догрузка отсека шаттла (algae 5/5) → «все отсеки полны, отправление» → шаттл закрылся и улетел (+40 XP), панель «Шаттл в рейсе, вернётся через 12:00». Ускорение прилёта добавлено (домен считает без ускорения) — после перекомпиляции будет 1:12 при ×10.
- `e2e3`: заказ дрона (Марк, столовая: реголит, соя, хлопок — товар довнесён тестом) → «ОТПРАВИТЬ» → дрон взлетел, сбросил груз у жилой башни, вернулся за 39 с → кредиты 44→144, XP 102→162.
- Наблюдения: заказы дронов при уровне 5 тяжёлые (6–7 позиций товара, которого на старте нет) — первый заказ невыполним без производства; допустимо по ТЗ (выброс/ожидание 22 мин), но для первых минут стоит мягче — отложено.
- Кадры `noch3/e2e/*.png`, монтажи `noch3-e2e-a/b/c.png`.

## 06:40 — огонь шаттла: найдена причина
- Факелы не строились: сопла считались по вершинам меша, а в Play вершины Tripo-меша без Read/Write недоступны — «кормовой срез пуст». Переведено на габарит корпуса (опора AABB вдоль курса), плюс ленивое построение при первом обновлении огня.
- Заголовок панели фабрики укорочен («свободно 0/2») и не заезжает под крест; кнопка дрона «В ПУТИ».

## 11:10 — огонь шаттла подтверждён кадрами (Х17 закрыт)
- Опора на мировой AABB дала квадрат 10,9×10,8 м (корпус стоит по диагонали) и увела «корму» на 2 м за срез — факелы висели в воздухе сбоку (кадры 45–47). Срез теперь по локальному боксу меша (`mesh.bounds` читается в Play всегда): длина корпуса 6,3 м, срез на 0,97 полудлины.
- Пять сопел по колоколам меша (3 сверху, 2 снизу), шаг 0,45 м, ряды через 0,5 м, кластер на 0,15 м к правому борту — подобрано по кадру 60 и подтверждено кадром 70 (все пять факелов внутри колоколов на снижении).
- Грабля: при hot reload Unity сериализует и приватные массивы ссылок — старые сферы пережили перезагрузку домена, новый строитель не запускался. Добавлен `PerestroitOgon()`.
- Хореография по кадрам 70–74: снижение — малые факелы; сел — все выключены; взлёт — напор (длина 1,3×1,3 м), разгон, уход. Кадры `noch3/e2e/70-74*.png`.

## 11:40 — спринт 8: ускорения (И-5/И-6, ТЗ 3.3)
- В каждой панели с таймером строка «Ускорить»: грядка и фабрика по `Economy.ProductionSpeedupCost` (остаток реального времени / time_scale → секунды каркаса; ≤ 30 с бесплатно), добыча — по ставке фабрик (в каркасе ставки добычи нет, допущение), шаттл в рейсе — `ShuttleSkipPrice` (И-6), пустой слот дрона — «Обновить» по лестнице `DroneRefreshPrice`.
- Изотопов на старте 30 (в каркасе стартового запаса нет — допущение); старые сохранения получают 30 один раз при загрузке (`startVydan`).
- Прогон (кадры `noch3/e2e/80-84*.png`): грядка «осталось 12 с» → «ЗА 10» → READY, изотопы 30→20; лёд «3 ледосбора работает, осталось 20 с» → «ЗА 18» → READY, изотопы 20→2. Цена льда 18 = 3 мин каркаса × 6.
- Хвост: в HUD нет счётчика изотопов (есть только кредиты и опыт) — баланс виден в строке ускорения; медальон изотопов — задача художнику интерфейса.

## 12:10 — спринт 10 (сохранение) и тесты домена
- Перезапуск Play посреди таймеров (батончик 30 с, вода 40 с, лёд 20 с, реголит 24 с при ×10): после загрузки `ends_at` всех слотов совпали до десятой секунды, кредиты 133, XP 228, изотопы 32, склад 47 — без расхождений; истёкшие за время Stop таймеры перешли в READY (офлайн засчитан). Файлы `noch3/e2e/sohranenie-do.txt` и `-posle.txt`. Критерий 4 выполнен.
- EditMode-тесты: 64 passed, 0 failed (`noch3/testy-domena.txt`). Грабля: запуск тестов из RunCommand вызывает перекомпиляцию и reload, слушатель результатов гибнет — вынесен в `Assets/Editor/TestyDomenaOtchet.cs` с `[InitializeOnLoad]` (меню Mars → Тесты домена). Критерий 5 в части тестов выполнен.
- Починено попутно: после hot reload список теплиц пуст (readonly-список не сериализуется) — `Obespechit` пересобирает; в домене 4 грядки при 3 теплицах в сцене — четвёртая без панели (guard по индексу), отмечено.
- Заказчик (12:00): иконки интерфейса делает сам — список нужного в `loop/ui/zakaz-ikonok-2026-09-06.md` (медальон изотопов, «ускорить», маркеры «готово/ждать», шаттл, дрон, строй-модуль).

## 12:25 — спринт 7: левелап с наградой (ТЗ 5.3)
- Уровень считался, но награда не выдавалась и всплеска не было. Теперь `Levelap`: за каждый достигнутый уровень `Levels.LevelUpReward(n)` — кредиты 100·N^1.2 к десяткам, 20 изотопов (+25 на «пустых»), всплеск в HUD: «Уровень N!» над медальоном уровня, «+кредиты» и «+изотопы» над монетой.
- Прогон: XP 228 → порог 249: уровень 6 → 7, +1030 кр, +20 изотопов (кадр `noch3/e2e/90-levelap.png`).

## 13:00 — сводка утра (ночь 3 закрыта)
**Кликается (проверено прогонами в Play, кадры в `noch3/e2e/`):**
1. Теплицы: посев → таймер → маркер «ГОТОВО» → сбор → склад, XP; «склад полон» — сбор недоступен.
2. Фабрики (пищевой, водная, атмосферный): рецепты с входами, очередь 2 слота, «в очереди: не хватает …», забрать.
3. Добыча: тап по ледосборам → три машины бурят (анимация вечера) → 3:20 при ×1 → забрать; реголит в яме 4 мин.
4. Склад: остатки, продажа, ёмкость (экран заказчика).
5. Шаттл: прилёт с огнём, стоит открытым с заказом, загрузка отсеков со склада, последний отсек → закрылся, огонь, улетел; XP и модули; возврат по таймеру рейса.
6. Дроны-курьеры: доска заказов, «Отправить» → полёт к куполу и возврат с кредитами/XP; «Выбросить», «Обновить» за изотопы.
7. Уровни: XP/уровень в HUD, левелап с наградой ТЗ 5.3 и всплеском.
8. Ускорения: «Ускорить» на всех таймерах по И-5/И-6, бесплатно < 30 с; изотопов на старте 30.
9. HUD и карточка цели — были, работают.
10. Сохранение: PlayerPrefs, таймеры по реальному времени, офлайн засчитан, перезапуск Play проверен.
Тесты домена: 64/64. Огонь шаттла и вечерние правки техники закрыты (Х17, Х20, Х22, Х24 и др. в `zamechaniya-2026-09-05-vecher.md`).

**Не сделано / ждёт заказчика:**
- Приёмка настоящими кликами (критерий 1 и 3) — за заказчиком; сохранение сброшено на чистый старт.
- Иконки: медальон изотопов, «ускорить», маркеры «готово/ждать», шаттл, дрон, модуль — заказчик делает сам, промпты в `loop/ui/zakaz-ikonok-2026-09-06.md`. До них: строка «Ускорить» без иконки, маркер над зданием текстом, изотопы видны только в строке ускорения.
- Текстильного модуля нет в сцене → ткань и комбинезон недоступны.
- Заказы дронов на старте тяжёлые (уровень заказов 5 → 6–7 позиций товара) — балансировать с заказчиком.
- В домене 4 грядки, в сцене 3 теплицы — четвёртая без панели.
- Открыты М4, М11, М12 (космонавт 07 на высоте, метанопровод, просвет грунта).
- Грабли ночи: hot reload хранит приватные массивы ссылок; Play без фокуса не крутит Update (включён Run In Background); вершины Tripo-мешей в Play не читаются.

## 13:30–15:00 — панели по референсу Township (заказчик: «меню выглядит ужасно»)
- Причина уродства: элементы комплекта клались как Simple, девятидольные пилюли/кнопка/панель растягивались картинкой. Теперь `ElementyHolsta`: Sliced с множителем «размер источника / размер на экране», как на складе.
- Заказчик прислал референсы (Молокозавод, Ткацкая фабрика) и сгенерировал по моим промптам две партии: иконки (модуль, дрон, шаттл, молния, пузыри «готово/ждать», медальон изотопов) и элементы конвейера (стойка, коробки, лоток, карточка с хвостиком, пилюля, карман). Вырезаны rembg, лежат в `Assets/Resources/UI/`.
- `PanelKonveyera` (теплицы, фабрики): лента, стойка со слотами-коробками (товар «в коробке», таймер-пилюля, полоса, галочка), лоток рецептов, карточка по тапу (входы «есть/надо», XP, время, кнопка). Добыча, шаттл и дроны — списочная панель (заказчик: конвейер для добычи неуместен).
- `MarkerZdaniya`: пузыри над всеми интерактивными объектами — простаивает (иконка того, что можно сделать), работает (часы + остаток), готово (галочка); тап по пузырю открывает панель. Ответ на «я бы не догадался, что дроны и машины можно запустить».
- Буровая 05 → метан (1:30 при ×10, партия 1–3), буровая 02 → железная руда, реголит — реголитосборщик в яме.
- Кнопка карточки цели теперь открывает теплицу цели (раньше висела без обработчика).
- Кадры: `noch3/e2e/140-puzyri.png`, `141-teplitsa-karta.png`, `131-led-spisok.png`, `132-burovaya-05.png`.
- Ждёт заказчика: маркер цели над теплицей (пин с глифом) — промпт выдан; надпись «BUILD KIT» на иконке модуля.
