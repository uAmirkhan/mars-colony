# Отчёт этапа 3 (спека loop/SPEC-ETAP-3.md), начат 2026-09-05 утром

## Блок 1 — полировка вида
- Аудит критика кадра записан: `loop/ui/AUDIT-polirovka-kritik.md`. Аудит арт-директора — в работе.
- Склад: элементы заказчика vkladka-neaktivnaya и kapsula-emkost подставлены (`etap3-sklad-elementy.png` = noch2-sklad-3.png на 11:24). Коммит mars-unity fcb313d.
- Пыль шаттла: три кандидата спрайт-листов (`loop/ui/pyl-kandidaty/`), выбран ohyhei Smoke Sprite Sheet (CC0), билборды-квады с флипбуком реализованы (`PolyotShattla.PylSpraytami`, спрайт-лист `Assets/UI/Effekty/pyl-klub-list.png`), но **невидимы**.
- **Системная находка:** в сцене MAIN не рендерится ни один прозрачный или alpha-clip 3D-материал (тесты квадов в Play: transparent Unlit, Sprites/Default, alpha-clip — невидимы; opaque Unlit с той же текстурой и красный куб — видны). Это причина и невидимых частиц. Depth priming выключен, маски -1, фич нет. Расследует `mars-shader-artist` (задача bd P1). До починки пыль шаттла ждёт.
- Аудит арт-директора записан (`AUDIT-polirovka-artdir.md`), оба аудита сведены в `SPISOK-pravok-blok1.md` (P1–P14, A1).
- Прозрачность: гипотезы шейдер-художника проверены в Play — ось сортировки (→Perspective), Volume/пост-обработка (off), copyDepthMode (→AfterOpaques) + intermediate (Auto): ни одна не вернула прозрачный квад; настройки возвращены. В сцене 22 рендерера с queue≥2450 (знаки cutout, выстилки Lit transparent) — проверить, видны ли они. Задача `mars-colony-4y9` с фактами. P12/P14 заблокированы.
- Виток UI-30 (P1+P2+P4: медальоны 1.65×, капсула опыта к краю, лента и кнопка карточки) — программист; A1 анимации — mars-juice параллельно (файлы не пересекаются).
- UI-30 (P1+P2+P4): кадр `etap3-hud-1.png` — медальоны 1.65× с выступом, капсула опыта у края; лента карточки: попытка уменьшить выступ числом сдвинула текст под ленту (высота ленты зашита в panel.png) — возвращено 68, кнопка 66/иконка 64/«4 XP» с отступом оставлены. Пересъёмка `etap3-hud-2.png`.
- A1 анимации (AnimatsiiInterfeysa, NazhatieKnopki, EkranSklada, SkladSostoyanie, ZhivoyInterfeys): в Play «+24» над капсулой кредитов при продаже, пузырь и нажатие — кадры `etap3-anim-*.png`.
- Приёмка UI-30 инспектором (`etap3-hud-2.png`): P2/P4/P5 подтверждены числом; P1 — медальоны вышли 1.36-1.44× вместо 1.6-1.7× (у спрайтов прозрачные поля ~17%); A1 по внешним скриншотам **не подтверждена** — на 60/130 мс кадры уже конечные (латентность клика+скриншота больше самой анимации).
- Доворот P1: `medH = kapH × 1.65 × 1.2` под поля спрайта, кадр `etap3-hud-3.png` (звезда видимая ~82 px при капсуле 50).
- UI-31 (P6 уступ вкладок, P7 пузырь над карточкой с клэмпом по полю, P8 отступ РАСШИРИТЬ): кадр `etap3-sklad-2.png`, клик в Play по руде — `etap3-sklad-2-puzyr.png` (пузырь вверх, центр по руде; перекрывает ряд выше — геометрически неизбежно при высоте 168 и шаге ряда 140).
- A1 пересъёмка серией кадров изнутри Play (`SeriyaKadrovUI.Nachat`, `loop/ui/anim/`): открытие/закрытие склада, пузырь, нажатие кнопки — движение есть и укладывается в спеку (пузырь сходится к 116 мс, нажатие 0-52 мс вниз / 71-108 мс вверх). Продажа вскрыла два дефекта, оба починены:
  1) «+N» над кредитами не двигалась и не гасла — `Dvigat` звался только из `Obnovit` (ColonyGame в сцене MAIN нет), всплывашка навек висела поверх числа. Теперь `LateUpdate` в `ZhivoyInterfeys`.
  2) Монета не появлялась в кадре ни разу при чистом логе — холст Screen Space Camera с масштабом ~0.0005, дуга `Vector3.up × 90` в мировых единицах уносила её за экран. Полёт переписан в локальных координатах холста (`prodat3/`: bbox монеты идёт от кнопки (497,496) к медальону (2079,77) за 340 мс).
- Коммит mars-unity 342627f. Приёмка UI-31 + P1 + A1 — инспектор (в работе).
- UI-32 (P3+P9): зазор меню—капсула уже 65 px (критик мерил старую раскладку), бар опыта 12→9 px и трек α0.7; нулевые товары склада: серый 0.42/α0.62, масштаб 0.9, число 8F7A5E; контраст самой светлой части нулевой иконки к полю ≈83/255 (порог 40). Кадры `etap3-hud-4.png`, `etap3-sklad-3.png`. Коммит mars-unity bcdd768. Инспектор — в работе.
- UI-33 (P10+P11): X плашки и маяка — экранный центр bounds рендереров здания (`VyborZdaniy.EkrannyyCentrX`), Y — макушка по вершинам как прежде; хвостик-треугольник `Assets/UI/Generated/hvostik-plashki.png` под плашкой (пивот плашки на макушке+12, остриё в 2 px над макушкой). Кадры окна: `play2_tap_kupol.png` (Теплица по центру среднего купола), `play2_tap_angar.png` (Склад по центру ангара, было ~57 px влево). Маяк на старте без тапа виден (`etap3-mayak-start.png`, по центру X целевого купола); кадр «через 1 с после Entered play mode» был ещё редакторским — стартовые кадры снимать не раньше 5 с после входа в Play. Коммит e85ecbe.
- Прозрачность (bd 4y9) **опровергнута**: пять тест-квадов (opaque, Unlit transparent, Sprites/Default, alpha-clip, Particles/Unlit transparent) видны и в редакторском RT (`diag-prozrachnost.png`), и в Play через бэкбуфер на 6 и 60 м (`anim/kvady/`). Пыль шаттла не видна по трём своим причинам, найденным серией кадров + тепловой картой диффа (`anim/shattl2..6`): (1) кольцо 1.5-3 м лежало под корпусом 8×7.6 м шаттла — квады закрыты самим шаттлом → кольцо и размер клубов от габарита (`RadiusShattla`); (2) лист ohyhei — альфа максимум 143, средняя 7.6 → лист собран из Kenney smoke_09 (CC0), 25 кадров одного клуба с гашением; (3) `_BaseMap_ST` через MaterialPropertyBlock у URP Particles/Unlit не применяется — на квад ложился весь лист 5×5 сеткой точек → окно кадра через UV меша. Тинт D9895A α0.65 на 75 м давал эффективную альфу ~0.2 → E8B48F α1.0 (отклонение от паспорта, показать АД).
- UI-34 (доводка + пыль): РАСШИРИТЬ +4 px (`etap3-sklad-4.png`), монета коэффициент 1.125 против 1.2 у звезды (`etap3-hud-6.png`), трек опыта α0.6; пыль шаттла видна (`anim/shattl7`, монтаж `etap3-shattl-pyl-montazh.png`: 7 клубов по кольцу снаружи силуэта, растут и держатся 0.5 с). Коммиты mars-unity 10e3a2c, 68da40c. Инспектор — в работе. bd 4y9 закрыта как опровергнутая.
- Приёмка UI-34 инспектором (`PRIEMKA-UI-34.md`): P1 принят (медальоны 1.64×), P3б принят (трек 9 px, контраст 1.1:1), P14 пыль принята (5 из 7 клубов видны, все вне корпуса, рост площади 0→6494 px за 505 мс, контраст к грунту 27-53/255); P8 НЕ принят — 25 px от нижней кромки против 30-35. Правка: кнопка РАСШИРИТЬ поднята ещё на 8 px (`barAbove + 8f`); кадр `etap3-sklad-5.png` (пересборка HUD в Edit mode 18:06): низ кнопки y=840, низ панели y=873 → 33 px (было 848/873 → 25), в коридоре 30-35 — P8 закрыт.
- UI-35, курс шаттла (замечание заказчика 05.09, скриншот с белой и красной дугой: двигатели сзади, вертикально не взлетает). `PolyotShattla.Poza` переписана: заход с левого края кадра по курсу носа (36 м, с 9 м над уровнем покоя, глиссада h∝ost², торможение s∝ost^1.5, 3.0 с), выравнивание 0.4 м / 0.15 с, просадка-баунс 0.45 с ровно в позу покоя; уход носом вперёд через карту (80 м, набор 35 м — высоту поднял `dron-1` с верхом 11 м, разгон s∝u^1.7, высота h∝u^1.8, 4.2 с) в правый верхний угол. Дистанции и высоты считаются по камере (WorldToScreenPoint до выхода за край) и рельефу (RaycastAll по zemlya), не хардкод. Нос — PCA вершин меша (0.829, 0, 0.560), это −up дочернего узла Tripo (0.2°); корпус 6.29 м, тонкий конец по +оси. Траектория от центра меша: пивот уехал на 24 м. Серии изнутри Play: `anim/kurs1-zahod` (шаттл входит у левого края на y≈490 из 1043 и садится в позу покоя), `anim/kurs3-vzlet`, `anim/kurs6-stoyanka`.
- UI-35, P12 тень-декаль: квад с процедурным радиальным спадом (1−r)^1.6, URP/Unlit transparent, чёрный, альфа 0.55 у земли → 0.14 на 12 м, диаметр 2.6 полуоси меша, растёт на 35% с высотой, лежит на уровне низа меша в покое (+6 см). Первый вариант (цвет через MaterialPropertyBlock, очередь 2999) при `isVisible=true` в кадр не попадал — тест-квады с тем же шейдером рисовались; причина не найдена, обход — как у тест-квадов (цвет в материале, очередь 3001). Тепловая карта `anim/kurs1-zahod/ten-heat.png`: затемнение под шаттлом 19-29/255 на глиссаде рядом с резкой солнечной тенью.
- UI-35, открытый отсек: второй ассет заказчика `shattl-otkrytyy` (цельный меш Tripo, дверей отдельными частями нет) выровнен в редакторе по курсу и низу меша закрытого (нос 0.3°, габарит 8.12×4.03×7.73 против 8.00×3.94×7.65). На стоянке через 0.6 с после касания закрытый подменяется открытым, перед взлётом — обратно (`PokazatOtkrytyy`, смещение снимается в Awake и следует за позой покоя). Настоящее раскрытие створок потребует разрезать открытый меш на створки в Blender (петля по нижней кромке) — отдельная работа кузнеца ассетов.
- Переименования (слово заказчика 05.09, `ZdaniyaCeliBuilder.SPISOK`): sklad-bunkery «Грузовой терминал» → «Склад», sklad-angar «Склад» → «Ангар спецтехники», modul-tonnelnyy «Общежитие» → «Туннельный дом». Три туннельных дома были одним объектом с одной целью клика (выделялись и подписывались вместе) — группа переименована в `tonnelnye-doma` без цели, цели поставлены на `modul-tonnelnyy-1/2/3`; имена перепрошиты на всех 28 целях, макушки запечены по вершинам. Иконка «Кофе-паёк»: пачка заказчика (rembg, предмет 45.7% кадра, без серого ореола и тени-подставки, обводка 14.5 px и насыщенность 0.64 как у ряда) заменила винный мешочек.
- Открытый вопрос: рабочая копия `Main.unity` получила правки сцены в 14:59 (трубы, болты, бочка кислорода, ящики, четыре новых баллона у двора; 70 объектов сдвинуто) — не мои команды и не мои агенты, происхождение не установлено; копия до сохранения — в scratchpad сессии; сцена сохранена вместе с ними при выравнивании шаттла и разделении туннельных домов.
