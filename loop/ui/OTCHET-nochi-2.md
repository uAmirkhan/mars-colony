# Отчёт ночного цикла 2 (2026-09-05 → 06)

Промпт: `loop/PROMPT-NOCH-2.md`. Кадры: `loop/ui/noch2-*.png`, `pilot-elementy-a.png`.

## Виток 1 — экран склада, каркас (UI-20, попытка 1: не принята)
- Собран каркас: панель, лента «СКЛАД», ёмкость «45/50», сетка 6×3 из 17 товаров, крест, скрим, открытие кнопкой меню; скрипт `Assets/Scripts/Game/EkranSklada.cs`, метод `InterfeysBuilder.KadrSklad`. Кадр `noch2-sklad-1.png`.
- Метрика: крем в рамке 120,120,1360,740 — 34.7% против ≥35 (не дошла).
- Инспектор: скрим фактически альфа ≈0.24 (яркость фона 0.76 от эталона) вместо 0.45; крест центр (1441,147) вместо стыка кромок (1470,121); нет вкладок, бара ёмкости, кнопки «Расширить»; третий ряд срезан краем кадра; контраст «0» на плашках 2.05:1.
- **Заказчик (≈02:00):** панель карточки цели для склада не годится (дубль и стиль), ячейки не годятся вовсе. Решение: склад строится заново по референсу амбара Township — отдельная панель `panel-sklad`, светлое поле `pole-sklad` без ячеек, предметы с числом под ними, вкладки, капсула ёмкости; промпты отданы (`loop/promty/promty-sklad-gemini.md`). До прихода картинок склад дальше каркаса не строится.
- Программист параллельно чинит скрим/порядок слоёв/крест (`noch2-sklad-2.png`) — эти правки переживут смену панели.

## Виток 2 — (в работе)
- Ученик: 7 кадров амбара Township в `loop/ui/referensy-sklad/` + `NABLYUDENIYA.md`. Подтверждено: ячеек нет, число голым текстом под иконкой, ёмкость текстовой пилюлей без бара, кнопка апгрейда зелёная компактная, вкладки — пиктограммы без подписей (у нас пока текст: значков категорий нет). Заказчик прислал panel-sklad, pole-sklad, vkladka-aktivnaya — вырезаны, в `Assets/UI/Elementy/`; программисту ушло ТЗ на перестройку (кадр `noch2-sklad-3.png`).
- Спека тапа по зданию (`wiki/saas/projects/mars-colony/spec-tap-po-zdaniyu.md`): механика уже реализована (VyborZdaniy, KonturZdaniya, ZhivoyInterfeys, BuildingClickTarget) — контур циан 2 px обратным корпусом, плашка процедурный Vizor 52 px. Дыры: блокировка при открытом складе, повторный тап, клэмп у края, анимация, кегль 24. Строка 2 промпта переписана: проверить кликом, довести на элементах заказчика в тёплом языке.
- План анимации шаттла (`loop/ui/plan-animatsii-shattla.md`, mars-juice): фазы, кривые, пыль, дрожь камеры, триггер, проверка тремя кадрами через `AnimationClip.SampleAnimation` в Edit Mode; два допущения помечены (знак вектора вглубь кадра, перевод 6 px в метры) — проверять тестовым кадром до кода.

## Виток 1, попытка 2 — склад на элементах заказчика (`noch2-sklad-3.png`)
- Перестроен по референсу амбара: panel-sklad, вкладки, pole-sklad, 17 товаров без ячеек (иконка 84 + число), «Ёмкость: 45 / 50», кнопка «РАСШИРИТЬ» (knopka-a), крест на углу, скрим 0.72 линейный (=40% падения яркости в sRGB-кадре). Все три ряда видны.
- Метрика: крем в рамке 120,120,1360,740 — 61.1% (порог ≥35) — дошла. Ореол вырезки вокруг поля (645 розовых px) снят эрозией альфы 3 px в `ikonka_rembg --erozia`; после пересборки 0 px.
- Баг по дороге: ключ кэша `ZalivkaSprite` не включал цвет — правки альфы молча брали старый PNG; исправлен.
- Инспектор — в работе; ворота закрываются двойной приёмкой.
- **UI-20 закрыта** двойной приёмкой (метрика 61.1%, инспектор: сломанного рядом нет). Оговорки → `bd` (контраст нулей, выцветание светлых иконок при 0).

## Виток 2 — кликабельность (UI-22, в работе)
- Проверка настоящим кликом в Play: выбор зданий работал и до правок (кадры `noch2-tap-1..3.png`). **Находка:** в MAIN.unity не было EventSystem — все кнопки uGUI (меню, ПОСЕЯТЬ, склад) были мертвы на реальный клик, batch-кадры этого не показывают; добавлен `ObespechitEventSystem()`. Второй баг: закрытие склада тапом по скриму выбирало здание под ним в тот же кадр — снимок состояния в LateUpdate.
- Доводка: контур #5FE3E8 → крем #FBEBBA; плашка названия на kapsula-a (зеркальная правая половина), кегль 26 с обводкой; повторный тап снимает; клэмп у края; тапы по миру блокированы при открытом складе. Кадры `noch2-tap-4.png`, `-5.png`.
- Метрика: циан в рамке 1200,520,400x400 (кадр 2590x1044) 5.906% → 0.000% (порог <0.05). Инспектор — в работе. Коммит mars-unity 8579f54.
- Спека `wiki/.../spec-tap-po-zdaniyu.md` устарела в двух пунктах (циан, Vizor) — обновить владельцу спеки.

## Виток 3 — критик кадра + анимация шаттла (UI-23, в работе)
- Критик (`mars-critic-kadr`): (1) карточка цели с «ПОСЕЯТЬ» остаётся полной яркости под открытым складом рядом с «РАСШИРИТЬ» — два зелёных акцента; скрывать карточку при открытом складе; (2) кремовый контур не читается на светлых куполах — нужна тёмная кайма 1 px под контуром; (3) плашка названия парит без привязки — хвостик к макушке выбранного меша; (4) вкладки «ТОВАРЫ/МОДУЛИ» близки по яркости — кайма/тень под неактивной (ждём vkladka-neaktivnaya заказчика). Не трогать: поле без ячеек.
- Правки 1–3 — виток 4 (после шаттла, Unity занят).
- **UI-22 закрыта** (инспектор: принимать). Оговорки → `bd`: якорь плашки на чужом объекте (90 px), маяк цели не виден ни в tap-кадрах, ни в pilot-elementy-a.png — проверить в Play.
- Шаттл (UI-23): код `PolyotShattla.cs`, `TryaskaKamery.cs`, `InterfeysBuilder.KadrShattl()` (программист без моста Unity — выполнял я через MCP). Первый прогон: пыль не работала — «Velocity curves must all be in the same mode» (оси x/z константы, y кривая) и бёрстов 12 при пределе 8; починено, но старая система пыли жила в сцене и не пересоздавалась — удалена, пересоздана (x/y/z Curve, 8 бёрстов). Кадры `noch2-shattl-1..3.png`: в воздухе, касание, взлёт; поза до/после (8.750, 4.804, -11.250) совпадает. Инспектор — в работе. Коммит mars-unity 50ba8ac.
- Урок: субагенты не имеют MCP-моста; при открытом редакторе они пишут код, а кадры снимает оркестратор через RunCommand. Объекты, созданные старым кодом, живут в сцене — при смене параметров пересоздавать явно.
- UI-23 попытка 1 — не принята: инспектор не нашёл шаттл в кадрах 1 и 3 (искал маской «белое» у площадки; шаттл на самом деле в воздухе над ангаром, около x=280,y=330, с тенью на грунте — проверено диффом с покоем: 18.5k отличных пикселей, bbox x123..514), но прав в главном — пыли при касании визуально нет. Попытка 2: пыль крупнее и плотнее, кадр касания на +0.3 с после контакта.
- UI-23 попытка 2 — **не закрыта, отложена**. Найдено и починено: эмиттер пыли стоял в пивоте модели, а пивот смещён от меша на 24 м (память «ставить по центру геометрии») — перенесён к центру меша; кривые частиц и бёрсты исправлены; старая система в сцене пересоздана. Но частицы не рендерятся в кадрах через `Camera.Render()` в RenderTexture ни в Edit, ни в Play (проверено тремя вариантами состояния и 4-метровыми белыми частицами; куб в той же точке рендерится). Живая проверка в Play скриншотами не удалась: латентность моста ~14 с ломает синхронизацию, клавиша L не дошла до Game view. Шаттл в детерминированных кадрах летает (тень на грунте читается). Утром: снять пыль реальным экраном в Play руками заказчика или через ScreenCapture.CaptureScreenshot из корутины.

## Виток 4 — контур, все здания, карточка при складе (UI-24, в работе)
- Контур обратно в циан #5FE3E8 с тёмной каймой #0E3B75 (два inverted-hull). Кадр со всеми контурами разом: `noch2-vydelenie-vse.png` — контур включился у 19 из 19 целей клика. Общежития сначала были без контура и без макушки: меш `modul-tonnelnyy.fbx` без Read/Write — включён; ещё 17 мешей зданий нечитаемы (макушка по габариту, контур работает).
- Без цели клика вовсе (по эвристике имени, решение заказчика): цистерны метана, солнечные поля/панели, площадка шаттла, башня-доминанта, kupol-grib, grib-antenna, bashnya-dominanta. Ответ на вопрос «все ли здания выделяются»: все 19 построек с ролью — да; декор и инфраструктура — нет.
- Якорь плашки — по макушке меша выбранного здания (BuildingClickTarget.MakushkaMir), карточка цели прячется при открытом складе (EkranSklada + KadrSklad).
- Контакт-лист временной камерой не отрисовался (пустой) — заменён полным кадром со всеми контурами.
- Кадры приёмки: `noch2-sklad-3.png` (карточка цели скрыта: зелёных вне кнопки 0.003% против 1.2 до), `noch2-tap-6.png` (клик по теплице в Play: контур циан+кайма, плашка над выделенным куполом, ~30 px до макушки в масштабе окна). Инспектор — в работе. Коммиты mars-unity ad94d95, 844b8e4.
- **UI-24 закрыта** (инспектор: принимать). Замечание: у жилой башни есть контур, хотя эвристика имён относила её к «без цели» — у неё есть роль, значит верно.

## Виток 5 — судья + нули склада и молнии (UI-25, в работе)
- Судья: V 77 (−3 контраст нулей, −10 маяк цели не виден при активной цели, −10 три фазы шаттла неотличимы без пыли), G 25/40 (посадка без обратной связи; продажа в складе не реализована, надпись «Нажмите на товар» врёт). Вердикт: полировали периферию. **Причина витков 6–8:** продажа товара степпером (ядро P0), пыль шаттла с живой проверкой — третья попытка закрывает или escalate; маяк цели в Play.
- UI-25: нули склада #7A6247 (контраст 5.07:1; программист честно отметил, что #8A7256 давал 4.03), нулевые иконки серые (насыщенность 7%), плотность поднята до 0.78 после замера рулона (31/255 при пороге 40); молнии выключены флагом `InterfeysBuilder.PokazyvatMolnii=false` (метки запечены в сцене, кода-генератора нет; 4 метки → 0 активных). Маяк цели при старте: ObnovitMayak звался только из выбора здания — добавлен вызов в LateUpdate; в Play при старте маяк всё ещё не виден — диагностика.
- Маяк цели: причина — `FindFirstObjectByType<MayakTseli>()` в Awake не видит выключенный объект маяка, ссылка всегда null; исправлено `FindObjectsInactive.Include` + вызов ObnovitMayak при старте. Кадр `noch2-start.png`: маяк над теплицей без кликов.
- Собственная ошибка витка: комментарий, вписанный в строку тернарного выражения, сломал компиляцию Editor-сборки молча (фильтр «error CS» ничего не показал, редактор жил на старой сборке, Play не входил — «Entered play mode» врал). Правило: после правки сверять время dll и cs, комментарии — только в конец строки после `;`.
- Замеры: контраст нулей 5.07:1; рулон ткани 45/255 (порог 40); молний 0 из 4. Инспектор — в работе.
- **UI-25 закрыта** (инспектор: принимать; замечание — маяк на шве между куполами → `bd`).

## Виток 6 — продажа товара степпером (UI-26, в работе)
- Продажа степпером собрана (`SkladSostoyanie.cs`, попап на pole-sklad, кнопки ±, ПРОДАТЬ на knopka-a, тултип для нулей; цены из `MarsColony.Domain.Config.Goods` — C#-зеркало goods.ts, не дубль констант). Настоящий клик в Play: попап открылся (`noch2-prodazha-1.png`), ± работают, продано algae x3 за 6 — запас 12→9, ёмкость 45→42 (`noch2-prodazha-2.png`). **Не дошло:** кредиты в HUD не изменились (лог «кредиты -1->-1»: ColonyGame.State в сцене нет) — чиню начисление.
- Ловушка окна: клик по координатам 1600x900, пересчитанным пропорцией на окно 2595x1041, промахивается — Canvas Scaler в широком окне масштабирует иначе; кликать по координатам со скриншота окна. Ошибка компиляции была видна только с `Types: All` (error CS приходит как Log) — до этого редактор молча жил на старой Editor-сборке.
- Кредиты починены: в сцене MAIN нет объекта ColonyGame (подтверждено предупреждением), склад ведёт кредиты сам и пишет в число HUD. Повтор кликом: `noch2-prodazha-1b.png` (степпер N=3, «Итого: 6 кредитов»), `noch2-prodazha-2.png` (запас 12→9, ёмкость 42/50, HUD 50→56), лог «[sklad] продано algae x 3 за 6: запас 12->9, кредиты 50->56».
- Тултип нулевого товара «Пока нет на складе» (`noch2-prodazha-3.png`) и закрытие попапа крестом (`-4.png`) — работают кликом. Критерий UI-26 выполнен по всем пунктам, инспектор — в работе. Коммит mars-unity ec8bfba.
