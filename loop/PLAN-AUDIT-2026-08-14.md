# Полный план исправлений и доработок — аудит 2026-08-14

> **ПОПРАВКА (тот же день, после сверки с параллельной сессией).** Новая
> витрина УЖЕ построена: сцена `ColonyOurs` (mars-unity) из собственных
> ассетов — сплошной меш земли, ярусы, колодец под карьер, посадка лучом.
> Цикл `loop/ours/` закрыт замером 6 на 88/100 (порог 90). Значит Блок 2
> ниже в части «бесшовная земля / карьер ниже нуля» уже СДЕЛАН — актуальный
> остаток по витрине это бэклог `loop/ours/score-6.md`:
> 1. **Корень всего (-8 стабильно): FBX-конвертер выгружает только tex_0,
>    вторая карта материала плиты дна карьера не доезжает до Unity.** Чинить
>    конвертер, а не двигать плиту.
> 2. Посадка плиты дна: самостоятельный подобъект, габариты никогда не
>    подгонялись под яму (-4).
> 3. Перезапечь `mars-ground.png` — ромбовидный узор тайлинга на дюне (-2).
> 4. Тень-декаль без объекта у теплицы-башни (-2).
> Пункты Блока 2 про купол-оранжерею/буровые/грунты остаются в силе как
> продолжение ассет-дорожки. Остальные блоки (0, 1, 3, 4, 5) не изменились.

Три аудитора прошли по веб-игре, 3D-витрине и служебному контуру.
Главная рамка: по решению владельца (run-8/plan.md) старая Unity-витрина на
ассетах Kenney/KayKit не чинится дальше — собирается НОВАЯ витрина на трех
ярусах из собственных ассетов. Все приоритеты ниже выставлены с учетом этого.

## Что здорово (не трогать)

- Веб-игра: все 8 критериев готовности среза закрыты, гейт 609/609,
  покрытие ~96%, мобильная проверка 100/100. Домен не менялся с прогона 6.
- Методология цикла (роли, рубрика, приемка числами) — работает.

## Блок 0 — зафиксировать состояние (сегодня, дешево, без решений)

1. Закоммитить mars-colony: отчеты run-7 (judge, tester-final, art-inspector-final),
   ВЕСЬ run-8 (включая решение владельца о пересборке), fundament.py,
   bake-ground-tex.py, landscape-research-2. Сейчас история решений
   существует только в рабочей копии. Плюс ветка на 3 коммита впереди origin.
2. Закоммитить Jarvis: wiki/log.md, backlog.md, два эпизода от 13.08.
3. Обновить wiki/meta/primer.md — он не знает ни про пересборку витрины,
   ни про космо-Township.

## Блок 1 — решения владельца (блокируют работу, нужен только Khan)

1. **Где живет задача ландшафта.** Эпизод 13.08 говорит «проект космо-Township
   не создан, фаза 0 не подтверждена», а работа над ландшафтом уже идет.
   Рекомендация: текущий ландшафт = фундамент НОВОЙ витрины Mars Colony
   (три яруса, бесшовная земля); космо-Township — отдельный проект, стартует
   только после явного «да» по фазе 0.
2. **Реестр: 15 открытых пунктов** (7, 10, 11, 14, 17-27) — код уже содержит
   решения исполнителя, нужно батч-ревью на «да»/правки. Самый заметный —
   п.23: изотопы двумя разными знаками, атом на Windows рисуется цветком.
3. **Вторая машина:** контрольный прогон TRELLIS подтвержден шаттлом
   (1536/50 работает). Дать отмашку пересоздать буровые 02-05 на этих
   настройках; буровая-01 — новая генерация с другого референса
   (профильный кадр без второго борта = причина «блина»).

## Блок 2 — новая витрина (основная работа)

Порядок важен: research → земля → проверки → модели → сборка.

1. **Дописать landscape-research-2/03-zemlya-3d-kazualok.md** — файл пуст по
   всем 5 вопросам, а именно он должен дать рецепт земли для 3D-казуалки.
   Без него новые ярусы соберут те же грабли (борт тайла, склейка стиля).
2. **Бесшовная земля вместо поклеточных тайлов** (рекомендации research
   06/07, не внедренные в SceneBuilder3D.cs):
   - placement grid — только данные, невидим;
   - один цельный меш земли на ярус, без бортов на границе клетки;
   - карьер — настоящий третий ярус НИЖЕ нуля (DeepTop), утопленный в
     рельеф, а не короб поверх грунта;
   - стыки ярусов прячутся декором, не подгонкой геометрии.
3. **Перенести готовые активы из run-7/8** (перечислены в plan.md):
   формулы проекции и заслона, четвертая проверка швов, measure-cold.mjs,
   конвейер glb-to-fbx.py с четырьмя ловушками.
4. **Закрыть дыру приемки:** объекты kind=terrain выпадают из всех трех
   автопроверок раскладки check-scene3d.mjs — на новых ярусах повторится
   тот же необнаруживаемый класс дефектов.
5. **Модели, в порядке цены:**
   - купол-оранжерея (не слеплен; закрывает «холодный акцент» — вторую по
     цене находку; лепится из икосферы, эталон утвержден);
   - второй тип грунта: пять grunt-*.glb НЕ спасать как тайлы — вырезать
     из них декор-пропсы (лед/руда/скалы) на новую цельную землю;
   - буровые 02-05 — пересоздать на 1536/50 (форма в норме, только
     поликаунт), 01 — новый референс;
   - шаттл — упрощение с 490k треугольников + решение по текстуре
     (плоские цвета против запеченной, решать по кадру).

## Блок 3 — инструменты приемки (2 красных теста вне гейта)

1. check-model.mjs молча пропускает дефект «плита» — 14 из 33 моделей
   партии, 4 ошибочно помечены «годны». Доказано красным тестом
   defects-run7-plita.test.ts.
2. Порог децимации 12000 не совпадает с планом ~20000 —
   defects-run7-polybudget.test.ts. Согласовать число и починить.
Оба дефекта нигде не всплывут сами: тесты сознательно исключены из гейта.

## Блок 4 — гигиена кода веб-игры (не срочно)

1. Property-тест на deficitlock.ts — отсутствует, держит шкалу C ровно на
   пороге 80 два прогона подряд.
2. Флап теста без диагностированной причины (−10 в run-7).
3. 142 lint warnings (0 errors) — по остаточному принципу.
4. Невоспроизводимость world_max.y карьера (−0.027 vs −0.077) — вероятно
   уйдет вместе со старой витриной, проверить после пересборки.

## Блок 5 — инфраструктура

1. **Beads актуализировать:** 9 задач — все open, ни одна не начата;
   находки run-7/8 не переносить дословно (обесценены пересборкой) —
   завести задачи Блока 2 как новые беды с зависимостями.
2. Скилл-паки из разведки (awesome-gamedev-agent-skills, blender-skills) —
   так и не поставлены; ставить, только если still нужны для новой витрины.
3. Godot MCP — не подключен; нужен только для космо-Township, ставить
   после решения по фазе 0.

## Что НЕ делать (обесценено пересборкой)

- Точечные фиксы старой сцены: карьер-короб, кристаллы-«конфетти»,
  рудный камень, баллоны, speederA под куполом, солнечные панели.
- Разбор 25+ неосмотренных подозрений sunk_into_prop/stacking старой сцены.
- Спасение пяти грунтовых тайлов как цельных тайлов.
- Латание check-build.mjs под старую 3D-ветку.
